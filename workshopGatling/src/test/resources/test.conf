#############################
# Env for test
#############################
conf {
  # front urls
  env {
    mainUrl = "https://www.templatemonsterdev.com/"
    serviceAuthorsUrl = "http://service-authors.templatemonsterdev.com/"
  }
  # loading (users and duration)
  loading {
    usersCount = 5
    duration = 10
  }
}